id;
"search-component" >
    Recipe;
Search < /h4>
    < input;
searchBox;
id = "search-box"(keyup) = "search(searchBox.value)" /  >
    /** @class */ (function () {
        function class_1() {
        }
        return class_1;
    }());
"search-result" >
     * ngFor;
"let recipe of recipes$ | async" >
    routerLink;
"/detail/{{recipe.id}}" >
    {};
{
    recipe.name;
}
/a>
    < /li>
    < /ul>
    < /div>;
